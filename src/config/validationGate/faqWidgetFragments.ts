/** C6 — scraped UI/widget fragments that must not appear in FAQs. */
export const FAQ_WIDGET_FRAGMENTS = [
  "what will fit",
  "easily switch sizes",
  "no credit card required",
  "join the waitlist",
  "sort by",
  "reviews",
  "web rate only",
  "standard rate",
] as const;
