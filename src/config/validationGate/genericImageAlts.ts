/** C3 — bare library generic alt strings that must never ship verbatim. */
export const GENERIC_LIBRARY_ALTS = [
  "vehicle storage",
  "boat storage",
  "rv storage",
  "truck storage",
  "business storage",
  "drive-up storage",
  "climate-controlled storage",
  "covered parking",
] as const;
